<template>
    <div class="ap-panel">
        <div class="ap-content" data-simplebar data-simplebar-auto-hide="true">
            <div class="ap-wrapper"><slot></slot></div>
        </div>
    </div>
</template>

<script>
    export default {
        name: 'APanel',
        data: () => ({

        }),
        watch: {

        },
        created() {

        },
        mounted() {

        },
        methods: {

        }
    }
</script>

<style>
    html, body { width: 100%; height: 100%; margin: 0; padding: 0; }
    .simplebar-track.simplebar-vertical { right: -3px; }
    .simplebar-track.simplebar-horizontal { bottom: -3px; }
    .ap-panel,
    .ap-vertical,
    .ap-horizontal {
        min-width: 20px;
        min-height: 20px;
        display: flex;
        flex-wrap: nowrap;
        align-items: stretch;
        align-content: stretch;
        touch-action: none;
        box-sizing: border-box;
        flex-grow: 1;
        flex-shrink: 1;
    }
    .ap-panel {
        padding: 4px;
        overflow: hidden;
        border: solid 1px;
    }
    .ap-content {
        width: 100%;
        height: 100%;
    }
    .ap-horizontal > .ap-panel {
        max-width: calc(100% - 20px);
    }
    .ap-vertical > .ap-panel {
        max-height: calc(100% - 20px);
    }
    .ap-horizontal {
        max-height: calc(100% - 20px);
        flex-direction: row;
    }
    .ap-vertical {
        max-height: calc(100% - 20px);
        flex-direction: column;
    }
    .ap-wrapper {
        white-space: nowrap;
    }
    .fullscreen {
        overflow: hidden;
        width: 100vw;
        height: 100vh;
        max-width: 100vw;
        max-height: 100vh;
        flex-grow: 0;
        flex-shrink: 0
    }
</style>
